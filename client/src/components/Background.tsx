import styled from "styled-components";
import {motion} from "framer-motion";

export const Background = styled(motion.canvas)`
  width: 100vw;
  height: 100vh;
  position: absolute;
  top: 0;
  left: 0;
`;
